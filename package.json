{
  "name": "blackbaud2calendar",
  "version": "1.0.0",
  "private": true,
  "description": "Puts Blackbaud tests, quizzes and major projects on Google Calendar (Google Apps Script).",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
